_docker_run()
{
    local cur=${COMP_WORDS[COMP_CWORD]}
    local images="$(docker images -q | awk '{print $1}')"
    COMPREPLY=( $(compgen -W "${images}" -- ${cur}) )
}

complete -F _docker_run docker-run