source /usr/share/bash-completion/completions/docker

_docker-run() {

    local previous_extglob_setting=$(shopt -p extglob)
    shopt -s extglob

    COMPREPLY=()
    local cur prev words cword
    _get_comp_words_by_ref -n : cur prev words cword

    local command='run' command_pos=0 subcommand_pos
    local counter=1
    while [ "$counter" -lt "$cword" ]; do
        case "${words[$counter]}" in
            run)
                return 0
                ;;
            -*)
                ;;
            =)
                (( counter++ ))
                ;;
            *)
                command="${words[$counter]}"
                command_pos=$counter
                break
                ;;
        esac
        (( counter++ ))
    done

    local docker_images=($(docker images --format "{{.Repository}}:{{.Tag}}"))
    local docker_images_no_tag=($(docker images --format "{{.Repository}}"))
    if [[ " ${docker_images[*]} " =~ " ${prev} " ]]; then
        COMPREPLY=$(docker inspect --format='{{range .Config.Cmd}}{{.}}{{end}}' $prev)
        COMPREPLY=($(compgen -W "${COMPREPLY[*]}" -- "${cur}"))
    elif [[ " ${docker_images_no_tag[*]} " =~ " ${prev} " ]]; then
        COMPREPLY=$(docker inspect --format='{{range .Config.Cmd}}{{.}}{{end}}' $prev:latest)
        COMPREPLY=($(compgen -W "${COMPREPLY[*]}" -- "${cur}"))
    else
        local completions_func=_docker_run
        declare -F $completions_func >/dev/null && $completions_func
    fi

    if [[ ${cur} == -* ]]; then
        COMPREPLY+=(
        "--cmd"
        "--help"
        "--image"
        "--mwd"
        "--no-gpu"
        "--no-gpu"
        "--no-it"
        "--no-name"
        "--no-rm"
        "--no-user"
        "--no-x11"
        "--verbose"
        )
        COMPREPLY=($(compgen -W "${COMPREPLY[*]}" -- "${cur}"))
    elif [[ ${prev} == "--name" ]]; then
        COMPREPLY=$(docker ps --format '{{.Names}}')
        COMPREPLY=($(compgen -W "${COMPREPLY[*]}" -- "${cur}"))
    fi

    eval "$previous_extglob_setting"
    return 0
}

complete -F _docker-run docker-run